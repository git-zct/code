//快慢指针
struct ListNode* middleNode(struct ListNode* head){
       struct ListNode* p=head,*q=head;
        while(q&&q->next){
            q=q->next->next;
            p=p->next;
        }
        return p;
}
